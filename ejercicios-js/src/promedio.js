
    let a=parseFloat(prompt('ingresa el primer numero: '));
    let b=parseFloat(prompt('ingresa el segundo numero: '));
    let c=parseFloat(prompt('ingresa el tercer numero: '));
    let d=parseFloat(prompt('ingresa el cuarto numero: '));
    function promedio(a,b,c,d){
        let promedio1=(a+b+c+d)/4;
        alert(`La promedio de lo numeros es ${promedio1}`);
    }
    promedio(a,b,c,d);
